/*
Construye un código bajo las siguientes reglas
- Declara un arreglo vacio. Con un ciclo while que se ejecute 5 veces, agrega los elementos al array

*/

let array = [];
let i = 0;

while(i < 5){

    array.length = (i);
    console.log(array.length);

    i++
}
